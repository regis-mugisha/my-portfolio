import { ImageOff } from "lucide-react";

const ImagePlaceholder = () => {
  return (
    <div className="w-full h-47 flex justify-center items-center border-2 rounded-2xl">
      <ImageOff size={70} />
    </div>
  );
};

export default ImagePlaceholder;
